prefix: ./env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - gpy
  - eccodes
  - cf-python
  - cf-plot 
  - udunits2
  - pip:
    - -r package.txt
    - -r dev-requirements.txt
